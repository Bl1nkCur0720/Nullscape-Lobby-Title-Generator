# Nullscape Lobby Title Generator 项目介绍

> 一个为 Roblox 游戏《Nullscape》玩家生成大厅标题（Rich Text）的纯前端网页工具。

---

## 一、项目简介

**Nullscape Lobby Title Generator** 是一款面向 **Nullscape**（Roblox 平台游戏）玩家的在线工具，用于快速生成带有渐变、彩虹、描边等效果的「大厅标题」富文本（Rich Text）代码。

在 Nullscape 中，大厅标题（Lobby Title）通常是一串带有颜色与字体样式的富文本。手动拼接 Roblox Rich Text 标签（`<font>`、`<stroke>`、`<b>`、`<i>` 等）既繁琐又容易出错。本工具通过可视化的操作面板，让用户**实时预览并一键生成可复制的 Rich Text 代码**，大幅提升制作效率。

### 核心特点

- 🎨 **所见即所得**：左侧调整参数，右侧实时预览最终效果。
- 📋 **一键复制**：自动生成 Roblox Rich Text 代码，点击即可复制。
- 🌈 **多种颜色模式**：纯色、渐变、彩虹三种模式，文本与描边均可独立设置。
- 🖋️ **40 款字体**：内置 Nullscape 常用的 Roblox 字体库。
- ✨ **丰富样式**：粗体、斜体、下划线、删除线、描边厚度与透明度。
- 🌐 **中英双语**：界面支持中文 / English 切换，并记住用户偏好。
- 📱 **响应式设计**：桌面与移动端均有良好显示效果。

---

## 二、技术栈

本项目为**零依赖、纯前端**实现，无需任何构建工具或框架：

| 技术 | 说明 |
| --- | --- |
| HTML5 | 页面结构、语义化标签 |
| CSS3 | Material Design 3 风格暗色主题、响应式布局、动画 |
| 原生 JavaScript (ES6) | 颜色算法、Rich Text 生成、事件处理、i18n |

- **无需安装依赖**：直接双击 `index.html` 即可在浏览器中运行。
- **无需后端**：所有逻辑均在浏览器端完成。
- **无外部资源依赖**：除字体通过 Roblox 资源引用外，页面本身不依赖任何 CDN。

---

## 三、目录结构

```
Nullscape-Lobby-Title-Generator-main/
├── index.html      # 页面结构 + 内置中英文翻译字典与语言切换逻辑
├── styles.css      # Material Design 3 暗色主题样式、响应式与动画
├── script.js       # 核心逻辑：颜色算法、Rich Text 生成、预设、事件绑定
├── README.md       # 原始简介
└── 项目介绍.md     # 本文档
```

---

## 四、功能详解

### 1. 文本内容

- 输入要生成标题的文本（默认示例为 `ENDURANCE`）。
- 实时显示字符计数，**最大长度限制为 50 字符**。

### 2. 文本颜色

支持三种颜色模式：

| 模式 | 说明 |
| --- | --- |
| **纯色（Solid）** | 所有字符使用起始色统一填充 |
| **渐变（Gradient）** | 从起始色到结束色，按字符逐个线性插值 |
| **彩虹（Rainbow）** | 按 HSL 色相从 0° 到 300° 自动分配，实现彩虹效果 |

- 起始色与结束色均支持**取色器选择**与**十六进制文本输入**（如 `#87292B`）。
- 底部渐变预览条会随设置实时更新。

### 3. 描边设置（Stroke）

- 可通过「启用」开关独立控制描边是否生效。
- 描边颜色同样支持纯色 / 渐变 / 彩虹三种模式。
- **厚度（Thickness）**：0.5 ~ 6（步长 0.5）。
- **透明度（Transparency）**：0 ~ 1（步长 0.05）。
- 未启用时描边区域呈半透明禁用状态。

### 4. 字体

内置 **40 款字体**，涵盖 Nullscape / Roblox 常用字体（如 Builder Sans、Fredoka One、Press Start 2P、Luckiest Guy 等），以 Roblox 字体资源路径形式引用：

```
rbxasset://fonts/families/{FontFamily}.json
```

### 5. 文本样式

支持四种可叠加样式开关：

- **粗体（Bold）** → `<b>`
- **斜体（Italic）** → `<i>`
- **下划线（Underline）** → `<u>`
- **删除线（Strikethrough）** → `<s>`

### 6. 实时预览

右侧预览区域通过将 `<stroke>` 标签转换为 CSS `-webkit-text-stroke` 样式，模拟 Roblox 中描边的实际视觉效果，做到「所见即所得」。

### 7. Rich Text 代码生成与复制

工具将每个字符包裹为独立的 `<font>` 标签，例如：

```html
<font color="#87292B" family="rbxasset://fonts/families/BuilderSans.json">E</font>
<font color="#9E2F33" family="rbxasset://fonts/families/BuilderSans.json">N</font>
...
```

若启用描边，则进一步包裹：

```html
<stroke color="#692B64" thickness="2" transparency="0">
  <font color="#96CBFF" family="rbxasset://fonts/families/BuilderSans.json">2</font>
</stroke>
```

最终再按样式开关包裹 `<b>` / `<i>` / `<u>` / `<s>`。

**复制**支持两种方式：
1. 优先使用现代浏览器 API `navigator.clipboard.writeText`；
2. 失败时回退到 `document.execCommand('copy')` 兜底。

### 8. 预设示例

提供 4 个一键加载的预设，点击即可套用完整参数：

| 预设 | 说明 |
| --- | --- |
| **ENDURANCE** | 红→亮红渐变，Builder Sans |
| **CELESTIAL** | 洋红→粉红渐变，Builder Sans |
| **20 Gliders** | 蓝色纯色 + 紫色描边 + 粗体 |
| **VBS**（VOIDBOUND SCARF） | 灰紫→浅紫渐变 + 粗体 |

### 9. 重置

一键将全部参数恢复为默认值（ENDURANCE 示例）。

### 10. 多语言（i18n）

- 通过页面顶部 🌐 按钮切换**中文 / English**。
- 使用 `data-i18n` 与 `data-i18n-placeholder` 属性标记可翻译文本。
- 语言偏好通过 `localStorage` 持久化，下次访问自动恢复。

---

## 五、核心实现原理

### 1. 渐变颜色插值（`lerpColor`）

在 RGB 色彩空间中，对起始色与结束色进行线性插值：

```
r = round(c1.r + (c2.r - c1.r) * t)
g = round(c1.g + (c2.g - c1.g) * t)
b = round(c1.b + (c2.b - c1.b) * t)
```

其中 `t = i / (len - 1)`，即第 `i` 个字符在整段文本中的位置比例。

### 2. 彩虹颜色（`hslToHex`）

将 HSL 颜色转换为 RGB 十六进制。字符按 `hue = (i / len) * 300` 遍历 0°~300° 色相，形成红→蓝→紫的彩虹渐变。

### 3. Rich Text 生成（`generateRichText`）

遍历输入文本的每个字符，为其生成独立的 `<font>` 标签（包含颜色与字体），再按需包裹 `<stroke>` 与样式标签，最终拼接成完整代码。

### 4. 描边预览转换（`convertStrokeForPreview`）

Roblox 的 `<stroke>` 标签无法在浏览器中直接渲染，因此通过正则匹配将其转换为 CSS 的 `-webkit-text-stroke` 属性，实现近似预览。

---

## 六、如何运行

1. 克隆或下载本项目到本地。
2. 直接用浏览器打开 `index.html`（推荐 Chrome / Edge / Firefox 最新版）。
3. 无需安装任何依赖或启动服务器。

> 提示：部分浏览器在 `file://` 协议下可能限制剪贴板 API，此时工具会自动回退到兼容方案，功能不受影响。

---

## 七、已知限制与注意事项

- **字符长度上限 50**：超出部分会被自动截断，避免生成过长的代码。
- **预览为近似效果**：浏览器中描边采用 `-webkit-text-stroke` 模拟，与 Roblox 引擎中的实际渲染可能存在细微差异，最终效果以游戏内为准。
- **字体依赖 Roblox 资源**：字体通过 `rbxasset://` 引用，仅在 Roblox 环境下有效，浏览器预览时不会加载真实字体文件。
- **渐变基于 RGB 插值**：某些颜色对在 RGB 空间中过渡的视觉效果可能不如 HSL 空间柔和。

---

## 八、作者与致谢

- **作者**：bl1nkCur
- **Made with ♥**

本项目为开源免费工具，欢迎提交 Issue 或 Pull Request 提出改进建议。
